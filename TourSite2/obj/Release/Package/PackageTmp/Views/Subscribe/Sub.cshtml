@model TourSite2.Models.FormsAuthProvider

<style type="text/css">
    .order_form {
        margin-left: 50px;
        margin-top: 20px;
        margin-bottom: 20px;
    }
</style>

@using (Html.BeginForm("SubMail", "Subscribe", FormMethod.Post, new { @class="form-horizontal", role="form" }))
{
@Html.AntiForgeryToken()
@Html.ValidationSummary(true)
     <legend>Оформление подписки</legend>
<form class="form-horizontal" role="form">
   
    <div class="form-group">
        <label for="formGroupInputLarge1" class="col-sm-2 control-label">Имя</label>
        <div class="col-sm-3">
            @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder = "Полное имя", tabindex = 1 })
            @Html.ValidationMessageFor(m => m.Name)
        </div>
    </div>
    <div class="form-group">
        <label for="formGroupInputLarge2" class="col-sm-2 control-label">Email</label>
        <div class="col-sm-3">
            @Html.TextBoxFor(m => m.MailAdress, new { @class = "form-control", placeholder = "Email", tabindex = 2 })
            @Html.ValidationMessageFor(m => m.MailAdress)
        </div>
    </div>
    <div class="form-group">
        <label for="formGroupInputLarge3" class="col-sm-2 control-label">Телефон</label>
        <div class="col-sm-3">
            @Html.TextBoxFor(m => m.Phone, new { @class = "form-control", placeholder = "Телефон", tabindex = 3 })
            @Html.ValidationMessageFor(m => m.Phone)
        </div>
    </div>
 
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-success btn-lg">Отправить</button>
        </div>
    </div>
</form>
}

    @*<div class="order_form">
        <h3>Подписка</h3>
        <form method="post" action="/Subscribe/SubMail">
          
            <table>

                <tr>
                    <td><p>Имя</p></td>
                    <td>@Html.TextBoxFor(m => m.Name) </td>
                </tr>
                <tr>
                    <td><p>Телефон </p></td>
                    <td>@Html.TextBoxFor(m => m.Phone) </td>
                </tr>
                <tr>
                    <td><p>E-mail</p></td>
                    <td>@Html.TextBoxFor(m => m.MailAdress)</td>
                </tr>
                <tr><td><button type="submit" class="btn btn-success">Подписаться</button> </td><td></td></tr>
            </table>
        </form>
    </div>*@
