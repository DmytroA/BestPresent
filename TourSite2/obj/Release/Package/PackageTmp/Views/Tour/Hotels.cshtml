@model IEnumerable<DataLayer.Hotel>
<h2>Каталог отелей</h2>
<table>
    <tr>
        <th>Id</th>
        <th>Отель</th>
        <th>Страна</th>
        <
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HotelID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country.Name)
            </td>
            <td></td>

        </tr>
    }
</table>
<p>
    @Html.ActionLink("Каталог отелей", "ListHotels")
</p>